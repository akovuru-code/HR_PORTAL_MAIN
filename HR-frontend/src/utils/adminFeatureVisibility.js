// Temporary Admin UI feature switches. Set a value to true to restore the
// corresponding UI without changing its route, API, or implementation.
export const ADMIN_FEATURE_VISIBILITY = Object.freeze({
  supportTickets: false,
  employeeStatusReport: false,
});
